# ---- Xcode / Swift ----
# Generated by XcodeGen — never commit; run `xcodegen generate` after pulling.
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
*.xcuserstate
*.moved-aside
.swiftpm/
.build/
Package.resolved

# ---- macOS ----
.DS_Store

# ---- Python (analysis/) ----
__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- Recorded sensor data (large / binary, not for git) ----
analysis/data/*
!analysis/data/.gitkeep

# ---- dataset/ ----
dataset/dataset.sqlite                 # rebuilt artifact
# Raw data: COMMIT small text exports (WL .txt) so derived rows stay reproducible;
# keep large binary/IMU streams (watch/AirPods .csv, video) OUT of git.
dataset/raw/*
!dataset/raw/.gitkeep
!dataset/raw/*.txt
